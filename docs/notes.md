# Cursor Task Instruction Note

Your (Cursor) task:

1. **Read the task:**  
   `docs/tasks/01_front-ui.md`

2. **If the task type = parent:**

   - Analyse all linked subtasks.
   - Do **not** write code.
   - Prepare to start with the _first subtask only_ after approval.

3. **Analyse the repository:**

   - Understand current file structure
   - Identify relevant frontend architecture
   - Check existing JS, Vue, CSS folders
   - Detect potential conflicts or missing structure

4. **Create a new file:**  
   `docs/tasks/01-01_to-do.md`  
   The file must contain:

   - Action plan
   - Refinements to requirements
   - Suggested order of execution
   - Any missing context or technical notes

5. **Wait for approval** before starting implementation.
   - If task type = **parent** → only start **first subtask** after approval.
